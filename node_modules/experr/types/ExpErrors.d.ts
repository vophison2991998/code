/// <reference types="qs" />
import { Request } from "express";
import { AppErr } from ".";
declare const _default: {
    targetNotFound(req: Request<import("express-serve-static-core").ParamsDictionary, any, any, import("qs").ParsedQs, Record<string, any>>): AppErr;
    signInFaild(): AppErr;
    noQery(msg: string): AppErr;
    noDocumentMatched(msg: string): AppErr;
    emailAlreadyExists(email: string): AppErr;
    notAuth(): AppErr;
    serverError(): AppErr;
    authFaild(): AppErr;
    validationError(msg: String): AppErr;
};
export default _default;
